# Base URLs
NEXT_PUBLIC_APP_URL=https://yourdomain.com
NEXT_PUBLIC_BASE_URL=https://yourdomain.com

# Database
POSTGRES_URL=postgres://user:password@host:5432/database
# Format: postgres://USER:PASSWORD@HOST:PORT/DATABASE
# For Vercel Postgres: Use the connection string from Vercel dashboard
# For Neon: Use the connection string from Neon console

# Authentication (Better Auth)
BETTER_AUTH_SECRET=
# Generate with: openssl rand -base64 32
# REQUIRED: Must be a secure random string for session encryption

# Google OAuth (Better Auth)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Get from: https://console.cloud.google.com/apis/credentials
# Authorized redirect URIs must include: https://yourdomain.com/api/auth/callback/google

# Stripe Payments
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
# Stripe Secret Key: Get from https://dashboard.stripe.com/apikeys
# Webhook Secret: Get from https://dashboard.stripe.com/webhooks after creating webhook
# Webhook endpoint: https://yourdomain.com/api/stripe/webhook
# Events to listen: checkout.session.completed, customer.subscription.created, customer.subscription.updated, customer.subscription.deleted

# Optional: AI Features
OPENAI_API_KEY=
# Required only if using chat features

# Optional: Security
RATE_LIMIT_BYPASS_SECRET=
# Generate with: openssl rand -base64 32
# Used for internal testing to bypass rate limits

# Optional: Analytics/Monitoring
# Add your analytics keys here if needed
# NEXT_PUBLIC_GA_ID=
# SENTRY_DSN=
